# Database Configuration
DB_ROOT_PASSWORD=password
DB_HOST=db
DB_PORT=3306
DB_NAME=corrector_despa
DB_USER=corrector
DB_PASSWORD=corrector123

# Ollama Configuration
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=phi3

# Extraction Method Configuration
# Options: "ocr" (text extraction) or "vision" (image-based with multimodal model)
EXTRACTION_METHOD=ocr

# Vision Model Configuration (only used when EXTRACTION_METHOD=vision)
VISION_MODEL=qwen2.5-vl:3b

# Language Configuration
# Language for LLM responses (reasoning, explanations, etc.)
# Options: "es" (Spanish), "en" (English), "pt" (Portuguese), etc.
RESPONSE_LANGUAGE=es

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Corrector de Documentos

# Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Timezone Configuration
TIMEZONE=America/Argentina/Buenos_Aires

# Error Handling Configuration
# Options: "production" (hide error details), "development" (show full error details)
ERROR_LEVEL=development
